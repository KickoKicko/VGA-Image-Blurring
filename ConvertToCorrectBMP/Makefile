# Makefile for bitmapTest

# Compiler
CC = gcc

# Output file
OUTPUT = bitmapTest

# Source files
SOURCES = bitmapTest.c referenceKernels.c

# Target to build the executable
$(OUTPUT): $(SOURCES)
	$(CC) -o $(OUTPUT) $(SOURCES)

# Clean up the generated files
clean:
	rm -f $(OUTPUT)
