# Makefile for bitmapTest

# Compiler
CC = gcc

# Output file
OUTPUT = 24bitmapProcessing

# Source files
SOURCES = 24bitmapProcessing.c 24bitReferenceKernels.c

# Target to build the executable
$(OUTPUT): $(SOURCES)
	$(CC) -o $(OUTPUT) $(SOURCES)

# Clean up the generated files
clean:
	rm -f $(OUTPUT)
