# Makefile for bitmapTest

# Compiler
CC = gcc

# Output file
OUTPUT = processing24bitmap

# Source files
SOURCES = processing24bitmap.c 24bitReferenceKernels.c

# Target to build the executable
$(OUTPUT): $(SOURCES)
	$(CC) -o $(OUTPUT) $(SOURCES)

# Clean up the generated files
clean:
	rm -f $(OUTPUT)
