# Makefile for bitmapTest

# Compiler
CC = gcc

# Output file
OUTPUT = processing8bitmap

# Source files
SOURCES = processing8bitmap.c 8bitReferenceKernels.c

# Target to build the executable
$(OUTPUT): $(SOURCES)
	$(CC) -o $(OUTPUT) $(SOURCES)

# Clean up the generated files
clean:
	rm -f $(OUTPUT)
